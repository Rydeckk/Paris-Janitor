import { Logement } from "../types/types";

interface LogementReservationProps {
    logement: Logement
}

export function LogementReservation({logement}: LogementReservationProps) {
    return (
        <div className="div_detail">

        </div>
    )
}